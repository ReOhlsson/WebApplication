@model  WebApplication.Models.Models.JsonProgramModel

<div id="popupMovie" class="overlay">
    <div class="popup">
        <h3>@Model.ProgramJson.title.sv</h3>
        <a class="close" onclick="closePopUp" href="">&times;</a>
        <div class="content">
            <hr class="footerhr" />
            Visas: @Model.ProgramJson.start - @Model.ProgramJson.stop<br><br>

            @Model.ProgramJson.desc.sv<br><br>
            @*@foreach (var category in Model.category.en)
            {
                <i>@category</i>
            }*@

        </div>
    </div>
</div>