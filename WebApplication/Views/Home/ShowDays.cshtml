@model WebApplication.Models.ViewModels.DaysVM

@foreach (var item in Model.ListOfDays)
{
    <li><a class="showDayShow" onclick="changeShowSchedual(this)" data-dateTime="@item.Date.ToShortDateString()">@item.Day @item.Date.ToShortDateString()></a></li>
}

<script>
    //function changeShowSchedual(days) {
    //    var date = days.getAttribute("data-dateTime");

    //    var channelName = ["svt1.svt", "tv3", "tv4"];

    //    $.each(channelName, function (index, value) {
    //        var $d = $("<div class='col-md-4 number'>").load('Home/Shows', { channel: value, date: resultDate });
    //        $('.containerChannel').append($d);
    //    });

    //    var channelName = ["kanal5", "tv6", "tv8"];

    //    $.each(channelName, function (index, value) {
    //        var $d = $("<div class='col-md-4 number'>").load('Home/Shows', { channel: value });
    //        $('.containerChannel2').append($d);
    //    });
    //}
    $(".showDayShow").click(function () {
            var date = days.getAttribute("data-dateTime");

            var channelName = ["svt1.svt", "tv3", "tv4"];

            $.each(channelName, function (index, value) {
                var $d = $("<div class='col-md-4 number'>").load('Home/Shows', { channel: value, date: resultDate });
                $('.containerChannel').append($d);
            });

            var channelName = ["kanal5", "tv6", "tv8"];

            $.each(channelName, function (index, value) {
                var $d = $("<div class='col-md-4 number'>").load('Home/Shows', { channel: value });
                $('.containerChannel2').append($d);
            });
    })
</script>
